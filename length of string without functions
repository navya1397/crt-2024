#include <stdio.h>
#include<string.h>

int main() {
    char str[50];
    int i,c=0;
    scanf("%[^\n]s",str);
    for(i=0;str[i]!='\0';i++)
    {
        c++;
    }
    printf("%d",c);
    return 0;
}
