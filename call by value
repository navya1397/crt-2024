#include <stdio.h>

void swap(int x,int y)
{
int t;
t=x;
x=y;
y=t;
}
int main()
{
    int a,b;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    printf("Values before swapping a=%d b=%d\n",a,b);
    swap(b,a);
    printf("Values after swapping a=%d b=%d",b,a);
    
}
